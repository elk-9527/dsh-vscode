# 验证：cordis 的 patch 能否按 id 覆盖一个已经存在的插件配置。
#
# 若可行，则以下两点成立：
#   1. 扩展启动后台内核时，可以按用户设置传入端口、服务商与模型
#      —— 当前这些设置对「自动启动路径」无效，属于设置项与实际行为不符；
#   2. 测试可以使用自己的端口，不要求 47821 空闲。
#
# 用法：dsh --profile dshdoor --patch <本文件> --no-open --host 127.0.0.1 --port 0

- id: acp-door
  config:
    port: 47833
