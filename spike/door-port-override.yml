# 验证：cordis 的 patch 能不能按 id 覆盖一个**已经存在**的插件配置。
#
# 如果可行，两件事就成立了：
#   1. 扩展拉起后台内核时，可以把端口/服务商/模型按用户设置传进去
#      —— 现在这些设置对「自动拉起的那条路」是无效的，属于设置骗人；
#   2. 测试可以用自己的端口，不必要求 47821 空着。
#
# 用法：dsh --profile dshdoor --patch <本文件> --no-open --host 127.0.0.1 --port 0

- id: acp-door
  config:
    port: 47833
