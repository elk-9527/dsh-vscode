node_modules/
dist/
out/
*.vsix

# M0 原始抓包可能包含文件内容，不入库；筛选后的帧另行放进 packages/acp/test/fixtures
spike/capture/
spike/scratch/

.DS_Store
*.log

# 打包产物（可随时用 npm pack 重新生成）
*.tgz

# 扩展的构建与测试产物：vsix、界面预览页、截图、无头浏览器临时配置
packages/vscode-extension/build/
