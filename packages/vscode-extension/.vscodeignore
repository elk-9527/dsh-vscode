# 不进入 vsix 的内容（市场包仅包含可运行的代码与市场需要读取的文件）。
#
# 本文件的作用：此前仅由自制的 tools/build-vsix.js 维护白名单（src、media、
# README、package.json）；改用官方 `vsce package` 后，缺少 .vscodeignore
# 会把测试、界面截图与诊断工具一并打入市场包，导致包体积成倍增长，
# 并公开内部文件。本文件为黑名单，与 build-vsix.js 的白名单互为兜底：
# 两条路径都会拦截。
#
# 核对最终打包内容的方法：npx @vscode/vsce ls --no-dependencies

# 测试与开发工具（仅用于运行测试，安装到用户机器上没有用途）
test/**
tools/**
build/**
shots/**
.vscodeignore

# 仓库级文件（市场页面使用 package.json 中的字段，不需要这些文件）
docs/**
*.log
*.tgz

# 编辑器与系统生成的文件
.vscode/**
.gitignore
.DS_Store
