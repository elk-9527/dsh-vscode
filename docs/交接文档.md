# 交接文档：VS Code 里的 DSH 面板（DSH Panel）

写给接手的人（人或 agent）。目标是让你**不用问原作者**就能继续把这个项目做下去，
并且不踩他已经踩过的坑。

仓库：`D:\dsh-vscode`（本地 git 仓库，分支 `main`）
上一轮的交接提交：`39ab8c7`（Workbuddy 接手后推到的位置）
**本轮（2026-09-19 凌晨）的改动见 `docs/明早先看这个.md`。**

> **动手之前先看 `docs/怎么恢复.md`。** 出问题的回退办法都写在那里，
> 包括"代码改乱了""`.git` 被删了""扩展装坏了""桌面档被改坏了"四种情况，
> 以及恢复点（标签 `v0.1-handoff` + `D:\dsh-backups\handoff-20260917-1427\`
> 下的整仓单文件备份）。**恢复流程已经实测验证过**（从 bundle 还原 → 零依赖
> 安装 → 测试通过）。

---

## 一、这个东西是什么，要达成什么

**一句话**：在 VS Code 的侧边栏里用你自己那套 DSH —— 不是另一个 agent，
而是同一个内核（同一份 `$DSH_HOME`、同一份记忆、同一套技能/插件、同一份会话记录），
只是不显示前台窗口。

**原始目标的五个子项**（照抄自任务书）：

1. 连接层：能连上正在运行的 DSH「门」；连不上时自动拉起后台 DSH（共用同一份
   `$DSH_HOME`，因此同一份记忆与会话记录）。
2. 侧边栏面板能完整对话：流式输出、工具调用展示、中断、模型/预设切换、上下文用量。
3. 自建测试与迭代循环，反复找 bug → 修 → 重测，直到稳定。
4. 装进 VS Code 并端到端验证通过。
5. 再做 UI 与性能优化。

**硬约束**（来自用户，务必遵守）：

- **绝不修改桌面端的 `desktop` profile**（那里的记忆/插件是用户在用的）。
  唯一允许的例外已经做完了：装了一个门插件（见下），并且知道怎么卸。
- 不删除用户的文件。
- 不破坏现有编译环境。
- 每完成一步提交 git。
- 用户不喜欢比喻、要"说人话"；不明确的地方**先问**，不要自己拍脑袋。

---

## 二、架构（三段式，理解它就能改任何地方）

```
VS Code 侧边栏（扩展 UI：webview）
      │  ACP：JSON-RPC，一行一个 JSON（NDJSON）
      ▼
  「门」插件 dsh-acp-door（装在 DSH 内核里的插件，在本机回环再挂一份 ACP 桥）
      │  默认监听 127.0.0.1:47821
      ▼
  正在运行的 DSH 内核（用户的桌面端，或者扩展自己拉起的无窗口内核）
```

**为什么要门这个东西**：DSH 内核本体不对外提供 ACP（它是给编辑器用的协议），
所以用一个内核插件在回环地址上再开一份 ACP。门**只监听回环、没有鉴权**
（这是一个已知的、需要向用户披露的安全边界：本机任何进程都能连上那个端口）。

**两种运行模式**（用户明确关心过）：

- 桌面端在跑 → 面板直接接入它（一个进程、一个大脑）。
- 桌面端没跑 → 面板用 `dshPanel.fallbackProfile`（默认 `desktop`）在后台拉起一个
  **无窗口**内核（`--no-open --host 127.0.0.1 --port 0`），连它干活，用完自己收掉。
  所以**不依赖桌面端在跑**。

**曾经确认过的一个硬限制（2026-09-17 已绕过）**：内核的 ACP 接口只提供
`initialize / authenticate / session/new / session/list / session/resume / session/close /
session/set_config_option / session/prompt` 和通知 `session/cancel`。
**没有 `session/load`** —— 意思是能列会话、能 resume（上下文能接回），但**没法把
历史对话的正文回放给面板**。绕过办法（用户 2026-09-17 已点头）：**门自己读
`$DSH_HOME/sessions`**（多帧 zstd 解码 + 回放重建，纯函数库在 `packages/dsh-door/lib/sessions.js`，
门 0.0.8 起以 `dsh-door/sessions/list|get` 两个旁路方法对外提供，请求由门就地应答、不转发内核）。

---

## 三、代码地图（每个文件干什么）

仓库根目录 `D:\dsh-vscode`：

| 路径 | 作用 |
| --- | --- |
| `packages/vscode-extension/` | **主体：VS Code 扩展** |
| `packages/vscode-extension/src/extension.js` | 入口：输出通道、注册视图、6 个命令、首次安装提示 |
| `packages/vscode-extension/src/panel/view.js` | 面板逻辑层（可脱离 VS Code 测试）：连接、会话、预设、附件、错误文案 |
| `packages/vscode-extension/src/dsh/session.js` | 会话层：ACP 会话封装、流式合并、切模型（**以内核回复为准**）、用量 |
| `packages/vscode-extension/src/dsh/sessions.js` | **历史会话读取（面板侧那份）**：多帧 zstd 解码 + 转录重建，只读；与门那份逐项一致，见 `test/sessions-parity.js` |
| `packages/vscode-extension/src/door/client.js` | 手写的 NDJSON JSON-RPC 客户端（零运行时依赖） |
| `packages/vscode-extension/src/door/locate.js` | 探测端口、拉起后台 DSH（Windows 上是 `cmd.exe /c "dsh ..."`）；**命令解析在这儿**（路径带空格，见第八节第 21 条） |
| `packages/vscode-extension/src/dsh/blocks.js` | 编辑器上下文拼块（选区正文 / 整个文件用 resource_link） |
| `packages/vscode-extension/media/*` | 界面三件套：`main.js` / `main.css` / `markdown.js` + `dsh.svg`（活动栏图标） |
| `packages/dsh-door/` | **门插件源码** + 构建出的 tgz（当前 `dsh-acp-door-0.0.9.tgz`） |
| `packages/vscode-extension/test/*` | 测试套件（见下） |
| `packages/vscode-extension/tools/*` | 打包、界面测试、截图、设计审计、**真编辑器自检** |
| `docs/明早先看这个.md` | 写给用户的说明（他自己的行动清单 + 已知坑） |
| `docs/UI-设计基线.md` | UI 美化的基线与待用户决定的 6 个问题 |
| `docs/第1步-装进桌面端.md` | 门插件是怎么装进桌面端的、怎么卸 |
| `packages/vscode-extension/shots/*.png` | 界面截图（**不入库**，用 `node tools/shots.js` 重新生成） |

**`build/` 里还有几个不入库的诊断脚本**（随手能跑、都不是测试套件的一部分；
`build/` 不受 `build-vsix.js` 影响，它清的是 `packages/vscode-extension/build`）：

| 脚本 | 回答什么问题 |
| --- | --- |
| `build/check-installed.cjs` | **用户跑的到底是不是当前源码**：装进 VS Code 的那份和仓库源码逐字节比对（`package.json` 里 VS Code 自己加的 `__metadata` 除外）。改完源码一定要重打包+重装，否则用户跑的还是旧代码 |
| `build/check-local-history.cjs` | 面板那条"自己读盘"的路在**这台机器上**看到了什么（几段会话、有没有解码失败、耗时、最近几段） |
| `build/who-owns-door.cjs` | 47821 上那个门**是谁开的**（桌面端自己的内核？还是测试留下的孤儿？） |
| `build/check-eol.cjs` | 有没有文件**混着 CRLF 和 LF**（`doorDrift()` 那种逐字节比对最怕这个） |
| `build/door-investigation/REPORT.md` | 「桌面档门 0.0.8 到底装没装」的调查记录与证据（结论见第七节第 4 条） |

**零运行时依赖**是刻意选择：手写 NDJSON 客户端、纯 CommonJS 扩展、ESM 门插件、
不用打包器。改的时候保持这个风格。

---

## 四、环境事实（照抄，别改）

| 项 | 值 |
| --- | --- |
| `$DSH_HOME` | `C:\Users\Lenovo\.dsh`（`profiles/` 在下面是关键：`profiles/desktop`） |
| VS Code | `D:\Microsoft VS Code\Code.exe`，版本 1.138.0 |
| 扩展安装位置 | `%USERPROFILE%\.vscode\extensions\local.dsh-panel-0.1.1` |
| 扩展清单版本 | `0.1.1`（`0.1.0` 是 Workbuddy 那轮；本轮修 bug + 界面收尾后提到 `0.1.1`） |
| 门插件源码版本 | `0.0.9`（`0.0.8` 加了历史会话旁路方法；`0.0.9` 是本轮修的两个 bug，见第七节） |
| 桌面档里装的门 | **`0.0.7`** —— 而且**升不上去**，原因见第八节第 20 条。面板已经**不依赖**它了（见第七节第 4 条） |
| 门监听 | `127.0.0.1:47821`（可用 `Get-NetTCPConnection -State Listen -LocalPort 47821` 看） |
| 卸载门插件 | `dsh plugin --profile desktop remove dsh-acp-door` |
| Shell | Windows PowerShell 5.1 风格：**没有 `&&`/`||`**；`Set-Content -Encoding UTF8` 会写 BOM（写 JSON 一律用 Node） |

**测试用的档**：`dshdoor`（测试专用）。所有测试都只动这个档，
**绝不碰 `desktop`**。生产路径（比如"兜底拉起"）才会用 `desktop`。

---

## 五、怎么跑、怎么验（命令都在这儿）

在 `packages/vscode-extension` 下执行：

```powershell
# 全套测试（14 个套件）。--all 会跑真进程/真内核那几层
node test/run-all.js --all

# 只跑快的（静态 + 纯函数 + 真进程 + 面板层，9 个套件）
node test/run-all.js

# 单个套件（注意「门的看帧判断」在门插件那个包里，不在本目录）
node test/static.js      # 清单/HTML id/协议/CSS 契约（46 项）
node ../dsh-door/test/frames.js   # 门的看帧判断，纯函数（93 项）
node ../dsh-door/test/sessions.js # 门的会话读取，纯函数（51 项）
node test/sessions-parity.js      # 两份会话读取实现是否一致（44 项）
node test/markdown.js    # Markdown 渲染（34 项）
node test/blocks.js      # 编辑器上下文拼块（42 项）
node test/session.js     # 会话层边界，假客户端（27 项）
node test/spawn-quote.js # 带空格的命令路径，真进程（16 项）
node test/fallback.js    # 兜底拉起真进程（9 项）
node test/resume.js      # 断线接回（5 项）
node test/presets.js     # 预设/模式（39 项）
node test/panel.js       # 面板层：假 vscode + 真门（91 项）
node test/smoke.js       # 端到端：真 DSH（31 项）

# 真浏览器里的界面断言（387 项，8 个场景）
node tools/uitest.js
node tools/uitest.js chat      # 只跑某个场景

# 真 VS Code 窗口里的自检（11 项；开隔离窗口，不碰用户的窗口）
node tools/vscode-check.js
node tools/vscode-check.js --keep     # 留窗口自己看

# 打包 + 安装
node tools/build-vsix.js
& "D:\Microsoft VS Code\bin\code.cmd" --install-extension "build\dsh-panel-0.1.1.vsix" --force
# 装完必须让用户 Reload Window（或重启 VS Code）

# 界面截图（改界面前后对比）+ 设计尺度审计
node tools/shots.js
node tools/design-audit.js
```

**哪些套件需要真实的模型额度**：`panel.js` 里"断线接回上下文"那一项、
`smoke.js` 大部分、`resume.js`/`presets.js`/`fallback.js` 里的真回合。
**模型额度用完时（内核回 429）这些会红，那不是代码坏了** —— 原文长这样：

```
回合失败：Internal error: turn failed: 429: {"type":"GoUsageLimitError",
"message":"5-hour usage limit reached..."}
```

**14. `fallback.js` 在桌面端开着的时候会自动跳过**（它需要 47821 是空的）。
跳过时 run-all 会打印 `⏭ 兜底拉起（真进程）（跳过：环境不满足）`，
退出码 2 表示"跳过"，不算失败。

所以断言总数有两种口径（2026-09-19 实测）：

- 桌面端**没开**（47821 空着）：**14 个套件全跑，915 项**；
- 桌面端**开着**（常态）：14 个里 13 个跑（兜底那 9 项跳过），**906 项**。

要完整跑兜底那一套，先把桌面端停掉，或者把测试指到别的端口。
**不要为了跑测试去杀用户的桌面端** —— 换端口或让用户自己决定。

---

## 六、当前状态：做完了什么（有证据的）

1. **连接层**：接入模式与兜底模式都在**真 VS Code 窗口**里验过（`tools/vscode-check.js`
   各 11 项）。2026-09-17 实测：桌面端在跑时**面板直接接入、没有另起内核**。
2. **面板功能**：流式输出、思考块、工具卡片、中断、模型下拉、预设下拉、上下文用量、
   权限询问、附件（选区正文 / 整文件 resource_link）、断线自动重连并接回上下文。
3. **测试**：**14 个套件**（桌面端开着时兜底那一套跳过），真浏览器 387 项，
   另有真进程/真内核/真编辑器三层。具体数字见第五节（每次改完都要更新那里）。
4. **装进 VS Code**：已安装 `0.1.1`，装上的文件与源码逐字节一致（`package.json` 因
   VS Code 自己加的 `__metadata` 而不同）。
5. **性能**：117 个流式增量受理 < 1ms；长对话 150 轮 → 1503 个 DOM 节点（线性）；
   已加回归护栏。历史会话本地读取：**86 段 1.34 秒**（2026-09-19 实测，
   含多帧 zstd 解码；界面在这一秒里显示"正在读取历史会话…"）。

**两次真机反馈暴露的问题（都已修）**：

- **受限模式（Restricted Mode）**：没声明 `capabilities.untrustedWorkspaces` 的扩展
  在受限模式里会被**整个禁掉且不报错**。已声明 + 加静态断言焊住。
- **找不到入口**：扩展激活了但面板从没被打开过（用户窗口日志里只有"启动"一行）——
  因为**没有"打开面板"的命令**。已补 `dshPanel.open` + 首次安装提示。

**2026-09-19 交接时抓到的 4 个真 bug（都已修 + 都有回归测试）**：

1. **`dshCommand` 路径里有空格就起不来**（第八节第 21 条）—— 两个独立缺陷叠在一起。
   本机 dsh 的真路径里就带空格，所以这是**真会踩到**的。
2. **门的预设队列会泄漏**（第八节第 24 条）—— 一次失败的建会话会污染下一次的预设。
3. **入站闸没有超时**（第八节第 25 条）—— 极端情况下消息静默消失。
4. **`listSessions()` 被复用后，端到端测试在验另一件事**（第八节第 22 条）——
   门一旧就红，而红的原因跟被测代码无关。

---

## 七、没做完的（按优先级）

1. ~~**错误提示改成中文人话**~~ —— **2026-09-17 已完成**。内核的报错不再原样贴给
   用户：先一句人话说清「发生了什么」，再一句说清「你能做什么」，然后才是内核原文
   （一字不删）。分类器在 `src/dsh/errors.js`（纯函数），改动与证据见
   `docs/第2步-错误提示改中文人话.md`。顺带修掉了 `dshCommand` 带参数时起不来的
   真 bug（见第八节第 12 条）。
2. ~~**历史会话列表**~~ —— **2026-09-17 完成门侧、2026-09-19 补齐面板侧**。
   门自己只读解析 `$DSH_HOME/sessions`（多帧 zstd），以 `dsh-door/sessions/list|get`
   旁路应答（需要门 ≥ 0.0.8）；面板顶栏新增历史按钮：浮层列表（标题/时间/回合数/
   工作目录/模式）→「回放」重建转录（工具卡默认折叠、**开头**注明"这是回放"）、
   「接回」先回放再走 `session/resume`，接不回上下文时明说。

   **2026-09-19 的补齐（重要）**：门那两条旁路方法在**用户实际用的那种模式**下
   用不上 —— 桌面档的门是 0.0.7，而它升不上去（见第 4 条）。所以面板现在
   **自己也能读盘**：`src/dsh/sessions.js`（CommonJS，与门那份逻辑逐项一致，
   由 `test/sessions-parity.js` 44 项拴住）。选择顺序是
   **门的方法优先（对远程门也对）→ 连的是本机时退回自己读**
   （`isLoopbackHost()` 判断，见 `view.js` 的 `readHistory()`）。
   实测：86 段会话、0 段解码失败、1.34 秒。
   测试：门侧纯函数 51 项、两份实现一致性 44 项、面板层 91 项（含 8.9 历史链路，
   跑在真门上）、浏览器 387 项（history 场景 62 项）。
3. ~~**UI 美化**~~ —— **2026-09-17 完成尺度与形态（提交 `21c9eac`），2026-09-19 收尾**。
   尺度收敛（间距 5 种全落 2px 刻度、字号 3 档、行高 2 档、圆角入令牌）、聊天形态、
   工具卡默认折叠（`grid-template-rows 0fr↔1fr` 180ms 动画）、顶栏加
   工作目录/回合时间戳+耗时；`prefers-reduced-motion` 全局兜底。
   `design-audit --strict` 通过；同轮顺手把面板内的加号删了（用户拍板只留
   标题栏那一个），兜底拉起改为候选命令逐个试（裸 `dsh` 不在 PATH 时自动
   退回默认安装位置的 `node bin.js`，不再必须先开桌面端）。

   **2026-09-19 收尾的三处**（都是"缺了就是缺了"的东西，不是审美偏好）：
   ① 历史浮层是盖住整个面板的，但**按 Esc 关不掉、焦点也不跟着走**
   （打开后焦点还在底下那些看不见的控件上，键盘用户按 Tab 就在空气里游走）；
   现在补了 Esc 关闭、打开时焦点进浮层/关闭时还给按钮、`role="dialog" +
   aria-modal`。② **读历史失败原来复用了"空状态"那套居中灰字** —— 会被读成
   "你没有历史会话"，而这两件事的出路完全不同；现在有独立的 `.history-error`
   （左对齐、警示色边框、`role="alert"`）。③ **回放原来滚到底部、且"这是回放"
   那句话在结尾** —— 改成停在开头并把提示放在**开头**（它要防的是"对着历史
   内容打字，以为在跟那段上下文说话"，这句提示越早出现越好）。
   `uitest.js` 相应加了 9 项断言（381 项）。
4. ~~**把桌面档的门升到 0.0.8**~~ —— **2026-09-19 判定为：不需要，而且现在也升不上去。**
   三件事必须说清楚，不然下一轮还会照着旧文档去撞：

   1. **那次升级其实没发生。** 2026-09-18 有人（Workbuddy）声称"桌面档门 0.0.8
      升级完成"并写进了文档和提交信息，但实际证据全是否定的：他留的
      `package.json.bak-door-upgrade`（11:55 的"升级前备份"）里那行依赖**也还是
      0.0.7**；pnpm store（`C:\Users\Lenovo\AppData\Local\pnpm\store\v11`）里
      有 0.0.1~0.0.5、0.0.7 每个版本的目录，**没有 0.0.8**；装着的
      `lib/index.js` 与 0.0.7 的 tgz 逐字节一致，且没有 `lib/sessions.js`。
      也就是说：**0.0.8 从来没有被 pnpm 装进去过**，那次"升级"没落地。
      后果是两条测试一直红着（见第六节末尾）。
   2. **`dsh plugin --profile desktop …` 现在会被拒**（第八节第 20 条）。
      09-17 能装成功，是因为当时桌面端没在跑。
   3. **面板已经不依赖门的版本了**（第七节第 2 条）：连的是本机时，
      历史会话由**面板自己读** `$DSH_HOME/sessions`（`src/dsh/sessions.js`，
      与门那份逻辑逐项一致，有 44 项一致性测试盯着）。所以升门**不再有功能上的必要性**。

   真要升（可选，收益很小）：源码头现在是 **0.0.9**
   （`packages/dsh-door/dsh-acp-door-0.0.9.tgz`），0.0.9 相对 0.0.8 修了两个门侧
   bug（预设队列泄漏、入站闸无上限）。步骤：**关掉 DSH 桌面端** →
   `dsh plugin --profile desktop remove dsh-acp-door` →
   `dsh plugin --profile desktop add file:D:/dsh-vscode/packages/dsh-door/dsh-acp-door-0.0.9.tgz`
   → 重启桌面端。**风险**：`add` 失败时依赖可能被清空（第八节第 15 条），
   那时桌面端内核不再加载门，面板会退化成"自己拉一个后台内核"（还能用，但不是
   一个大脑了）。所以要么不动，要么在用户在场时动。

5. **版本管理**：标签 `v0.1-handoff` 与整仓单文件备份已做好（见 `docs/怎么恢复.md`）；
   扩展版本号 `0.1.1`（2026-09-19；Workbuddy 那轮是 `0.1.0`，旧的
   `local.dsh-panel-0.1.0` 目录已被 VS Code 标成 obsolete、它自己会清）。
   剩下的：往远程推 —— 用户 2026-09-17 表态**先不推，功能做完再推**，
   届时要用户给地址与授权。
6. **测试会话污染**：测试在用户的共享会话库里留下了会话记录（`$DSH_HOME/sessions`）。
   2026-09-19 实测已有 **86 段**。已向用户披露，**没有**擅自删除。
   用户 2026-09-17 同意先扫只读清单、由他逐个挑。
   （清理工具在 `packages/vscode-extension/test/` 之外的那两个脚本里，见 git 历史
   `1f84994`/`3bb8566`：**只读盘点**与**按条件清理**，清理那个必须用户点头才跑。）

---

## 八、踩过的坑（照抄，别再踩）

1. **`build/` 会被打包脚本清空**：`node tools/build-vsix.js` 会 `rmSync(build)`。
   想留下的产物别放 `build/`（截图就因为这个丢过一次，现在放 `shots/`）。
2. **绝不用名字去 `taskkill` 内核**：用户自己的 DSH Desktop 拉起的内核命令行里
   也有 `--no-open`，跟扩展拉起的长得像。自检工具现在**一个内核都不杀**，
   只收自己开的隔离窗口，剩下的只报告（并排除"祖先是 DSH Desktop.exe"的进程）。
3. **扩展拉起的内核长这样**：`cmd.exe /d /s /c "dsh --profile desktop --no-open
   --host 127.0.0.1 --port 0"`；真正的 `DSH Desktop.exe` 子进程命令行里反而**没有**
   `--profile/--no-open`。要识别"是不是我们拉起的"，认那个 `cmd.exe`。
4. **无头 Chrome 不为程序化滚动派发 scroll 事件（实测 0 次）**。界面的
   "别把正在翻记录的人拽回底部"逻辑靠 scroll 事件判断，直接测会得出假 bug。
   `tools/uitest.js` 里是**手动派发**一个 scroll 事件来测，注释写清了原因。
5. **`tools/uitest.js` 里的断言脚本是模板字符串**：注释和代码里都不能出现反引号，
   换行必须写 `\\n`，否则生成出来的脚本直接语法错。
6. **PowerShell 引号/编码**：内联 `node -e` 写中文、`$()`、反引号极易被吞。
   写脚本或提交信息一律**用文件**（`git commit -F 文件`）。
7. **`code.cmd` 会把参数转给正在运行的实例**，不能用来开隔离窗口。要开隔离窗口必须
   直接起 `Code.exe`，带 `--user-data-dir` / `--extensions-dir` / `--new-window` /
   `--verbose`（没有 `--verbose` 时输出通道日志不落盘）。
8. **切换模型必须信内核回复**（`setSessionConfigOption` 返回的 `configOptions`），
   不能信自己请求的值 —— 内核会归一化。
9. **预设（agent preset）**：只有桌面端创建的会话会把 preset 写进会话记录；
   走门建的会话需要请求里带 `_meta['dsh-door']` 补挂，否则**恢复回来的会话没有工具**。
10. **`dsh` 命令不在 PATH 时**以前要干等 120 秒才报错。现在盯子进程：
    它一退出就立刻说清"命令错了、怎么办"。这条分支的测试覆盖率是靠给等待函数
    加一个只有测试用的超时参数才做到的。
11. **`$pid` 是 PowerShell 只读变量**，循环里别用它当变量名。
12. **`dshCommand` 里带参数会起不来**（2026-09-17 修）。`locate.js` 原来把**整串**
    命令当一个程序名加引号，于是 `node D:\...\bin.js` 变成 cmd 眼里「名字里有空格的
    程序」，退出码 1。现在先 `splitCommand()` 拆再逐段加引号。
13. **本机 `dsh` 命令不在 PATH 上**。npm / pnpm / nvm 的 bin 目录里都没有 `dsh.cmd`，
    唯一的 CLI 是 `C:\Users\Lenovo\.dsh\profiles\node_modules\@deepseek-ai\dsh\lib\bin.js`
    （v0.1.5-rc.2）。所以：
    - 扩展的兜底拉起要把 `dshPanel.dshCommand` 填成
      `node C:\Users\Lenovo\.dsh\profiles\node_modules\@deepseek-ai\dsh\lib\bin.js`；
    - 命令行跑需要真内核的测试时，先设
      `$env:DSH_PANEL_DSH = "<那个 node.exe 的完整路径> <bin.js 的完整路径>"`；
    - 不设的话，`panel.js` / `smoke.js` / `fallback.js` 这些会失败（**不是代码坏了**）。
14. **worktree 的换行符和主工作区不一致**。`D:\dsh-vscode` 工作区里的文件是 LF
    （上一轮用写文件工具写的），而新开的 worktree 按 `core.autocrlf=true` 检出成
    CRLF。git 会归一化所以两边 `git status` 看着都干净，但
    `test/helpers/door.js` 的 `doorDrift()` 是**逐字节比对** —— 在 worktree 里它会
    误报「门跟源码不一致」，然后试图用 `dsh plugin` 重装（再撞上第 13 条）。
    碰上门相关测试前先把 `packages/dsh-door/**` 统一成 LF。另外 `dshdoor` 测试档
    的门依赖指向的是 `D:/dsh-vscode/packages/dsh-door`，不是 worktree。
15. **沙箱会拦 pnpm 的批量删除**（2026-09-17 踩过）。`syncDoor()` 的 remove+add
    会触发 pnpm 换依赖文件，一换超过阈值（实测 184 个文件、阈值 50）就被
    `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]` 拦下 —— add 失败且**测试档
    的 `package.json` 依赖已被 remove 清空**，内核启动时不再加载门（症状：
    内核起来了、47821 永远不开）。`syncDoor()` 现在对 add 失败有兜底（按文件
    同步 lib + 手工补回依赖声明）。真要重装：先把旧依赖目录**改名挪开**再 add，
    pnpm 就不需要删东西，能成功。
16. **`probePort` 是单次探测，`waitForPort` 才是等门开的**。门要 10~40 秒才开，
    用 `probePort` 判断「门开没开」几乎必然假阴性（它只试一次，连接被拒就
    返回 false）。等门一律 `waitForPort(host, port, { totalMs })`。
17. **TransformStream 没人消费 readable 就整条管子是死的**（2026-09-17 踩过，
    门 0.0.8 曾因此哑掉）。出站中继最初写成 TransformStream 却没人读
    `relay.readable` —— transform 永远不执行，内核回复永远出不去，客户端
    看到的是「门接了线但不应答」。现在出站层是 `frames.js` 的
    `createOutboundRelay()`（返回 WritableStream，ndJsonStream 直接写它），
    并且有「写进来的行必须从 sink 出来」的测试盯着（`test/frames.js` 第 8 节）。
18. **`dsh` CLI 不许动 desktop 档**（2026-09-18 实测）：`dsh plugin --profile desktop
    remove/add` 直接被拒（`profile "desktop" is managed exclusively by the Electron
    application`）。升门只能手动改文件（做法见第七节第 4 条）。
19. **GNU tar 把 `D:` 当远程主机**：Windows 盘符路径（`tar -xzf D:/...`）报
    `Cannot connect to D: resolve failed`。路径写成 `/d/...` 就好。
20. **`dsh plugin --profile desktop …` 只在桌面端**没跑**时能用**（2026-09-18 实测）：
    桌面端跑着的时候，CLI 直接拒（`profile "desktop" is managed exclusively by the
    Electron application`）。09-17 那次装门之所以成功，是因为当时桌面端没开。
    所以"升级桌面档的门"这件事不能想当然，见第七节第 4 条。
21. **`spawn(cmd.exe, [...])` 会把参数里的引号再转义一遍**（2026-09-19 修，真 bug）。
    Windows 上要跑 `cmd.exe /d /s /c "完整命令行"` 时，如果字符串里已经带了引号
    （比如程序路径里有空格，你把它括起来了），默认的 spawn 会再转义一次，cmd 收到
    的就变成了坏掉的路径。必须同时给 `windowsVerbatimArguments: true`。
    另一个独立的坑：**不能只按空白拆命令**。本机 dsh 的真路径是
    `…\AppData\Roaming\DSH Desktop\host-commands\…\dsh.cmd`，路径里**有空格**，
    用户按说明"把完整路径填进设置"填的是裸路径，按空白拆会把程序名劈成两段，
    报的错是 `'C:\Users\…\Roaming\DSH' 不是内部或外部命令`。
    现在 `resolveCommand()` 会「优先认整串是一个存在的文件」，再退到
    「把拆坏的片段按磁盘上真实存在的路径重新粘回去」（`rejoinExisting`）。
    回归测试：`test/spawn-quote.js`（16 项，**真进程**）—— 它造一个带空格的
    `.cmd` 垫片，用真的 `spawnBackgroundDsh()` / `runDshSync()` 去跑，
    再读垫片的输出对内容。为什么必须真跑：这两个缺陷拼出来的字符串**看起来是
    完全正确的**，只断言 `commandLine()` 的返回值抓不住。这个套件在
    `run-all.js` 里，不需要 dsh、不占端口。
22. **同一个方法名被复用时，测试的断言会悄悄变成在验另一件事**（2026-09-19 修，
    真 bug）。`DoorClient.listSessions()` 本来是 ACP 的 `session/list`（返回
    `{sessionId, cwd}`），被改成走门的旁路方法（返回 `{id, title, turns}`）之后，
    `test/smoke.js` 里那段"验 ACP 列表形状"的**注释和断言都没跟着改** ——
    于是门一旧它就以一个和被测代码无关的理由变红。现在两个方法名字分开了：
    `listKernelSessions()` 是 ACP 的，`listHistory()`/`getHistory()` 是门的。
23. **两份实现必须有一条一致性测试拴着**（2026-09-19）。历史会话读取现在有两份
    （门 ESM 一份、面板 CJS 一份，因为扩展零依赖、不能 require 门的 ESM）。
    光靠"注释里写一句要同步"是拦不住的，所以有 `test/sessions-parity.js`：
    同一批会话文件喂给两份，44 项逐项比对，包括坏帧、截断、非法 id、缺目录。
24. **失败路径上的状态不清会污染下一次**（2026-09-19 修，真 bug）。
    `decorateLine()` 的预筛写着 `line.includes('"result"')`，于是**只有 error、
    没有 result** 的回复压根没被看：`state.queue` 里那条点名永远不清理。它是按
    "发出顺序"领的（`session/new` 时 sessionId 还不知道），于是**下一次**建会话
    会领到上一次失败时点的那个预设 —— 用户没点名却挂上了别的模式。
    修法：出错回复也要处理（把点名从队列里摘掉）。回归测试在
    `test/frames.js` 第 8 节第 (6) 段。
25. **"等一个 promise"必须有上限，否则故障是"消息静默消失"**（2026-09-19 修）。
    入站闸 `holdUntilMounted()` 原来是 `await waiting.catch(() => {})`，
    没有超时。挂载链内部全都 catch 过，唯一漏网的是内核某个服务返回**永不落定**
    的 promise —— 那时 `session/prompt` 被永久按住：用户发了消息，界面上没有任何
    反应，也没有任何报错可查。现在入站和出站用同一个上限（`MOUNT_WAIT_MS`，
    通过 `frames.js` 的 `waitForMount()`），到点放行，最坏退化成"这个会话手里没工具"。
26. **PowerShell 5.1 的 `Get-Content -Raw` 会把 UTF-8 读成乱码**，`ConvertFrom-Json`
    跟着解析失败（2026-09-19 连踩两次）。读 JSON 一律用 Node（`JSON.parse(fs.readFileSync)`），
    别用 PowerShell 的 JSON 命令。同理：内联 `node -e "…"` 里的引号/反引号极易被
    PowerShell 吞掉 —— **写成 `.cjs` 文件再跑**（临时脚本放 `build/`，它不入库）。

---

## 九、自检工具的口径（别把它当成"万能")

- `test/*` 大多用假客户端/假 vscode，快但不覆盖"人能不能找到入口"这种真机问题。
- `tools/vscode-check.js` 用 `DSH_PANEL_AUTOFOCUS=1` 让扩展**自己**打开面板，
  所以它验证了"激活 → 展开 → 握手 → 建会话"整条链，但**恰好绕过了"用户点得到吗"**。
  这正是早上出问题的地方：**自动化通过 ≠ 人能用**。人机交互环节最好也让用户试一次。
- 断言自己也要被验证：曾经有一条"消息间距一致"的检查写成"≥3 条消息才检查"，
  而场景里只有 2 条 —— 从来没跑过（死代码）。

---

## 十、和用户相处的规矩（他亲口说的）

- "有什么不明确的问题一定要来问我（用通俗易懂的话），不要自己想怎么搞怎么搞"。
- "不要用你那奇妙的比喻，说人话"。
- 对界面美观要求高；**UI 那一步必须等他给样稿**（他说过"这个能最后再做吗？
  如果可以的话就先放一下吧，记得要做的时候提醒我给你样稿"）。
- 不要删用户的东西（包括测试产生的会话记录）。
- 每次做完一步提交 git。
