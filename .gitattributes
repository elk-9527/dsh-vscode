# 换行与二进制的规矩：谁在这台机器上提交都一样，别让 CRLF 混进历史。
#
# 背景：这个仓库一路在 Windows 上开发，而门包的 package.json 曾经被
# PowerShell 悄悄加过 UTF-8 BOM，装进用户档之后内核 JSON.parse 直接崩
# （事故记录见 packages/vscode-extension/test/static.js 里那条 BOM 断言）。
# 换行/BOM 这类"看不见的字节"必须有明文规矩，不能靠"我记得"。
#
# text=auto 的含义：git 按内容判断是不是文本，文本一律以 LF 存进历史，
# 检出时再按平台转换。图片和包体明确标成二进制，避免被当文本处理。
* text=auto

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.vsix binary
*.tgz binary
*.zip binary
